## Customize dependencies
dependencies:
  pre:
    - npm install jasmine-node istanbul # install from a different package manager

## Customize test commands
test:
  override:
    - npm test --coverage && cat ./coverage/lcov.info | COVERALLS_REPO_TOKEN=bHO5dXqhvsDit2A9Rp1V1JbI13NATLicB ./node_modules/coveralls/bin/coveralls.js
